/*

Write a function that takes an array of strings as input and returns a new array where the first element is moved to the end of the array. You must use both shift and push methods to achieve this.

*/

/*

Write a function that takes an array of strings and an array of items to prepend as input. The function should return a new array with all of the items added to the front, in the order given. You must use the unshift method to achieve this.

Example:
prependItems(['x', 'y'], ['a', 'b', 'c']) returns ['a', 'b', 'c', 'x', 'y']

*/
