# Prompt 1: Reverse a list using append and pop
# Write a function that takes a list as input and returns a new list with the elements in reverse order. You must use both append and pop methods to achieve this. 

def reverse_list_append_pop(lst):


# Prompt 2: Move first N elements to the end of the list using remove and append
# Write a function that takes a list of integers and a number n as input. The function should return a new list where the first n elements are moved to the end of the list. You must use both remove and append methods to achieve this. 


def move_first_n_to_end(lst, n):