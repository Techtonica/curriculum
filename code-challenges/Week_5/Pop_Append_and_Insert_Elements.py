# Write a function that takes a list of strings as input and returns a new list where the first element is moved to the end of the list. You must use both pop and append methods to achieve this. Additionally, handle edge cases such as empty lists or lists with fewer than two elements.

def move_first_to_end(lst):



# Write a function that takes a list of strings and a list of items to prepend as input. The function should return a new list with all of the items added to the front, in the order given. You must use the insert() method to achieve this.

# Example:
# prepend_items(['x', 'y'], ['a', 'b', 'c']) returns ['a', 'b', 'c', 'x', 'y']


def prepend_items(lst, items):