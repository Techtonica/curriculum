const submitBtn = document.getElementById("submit");

submitBtn.addEventListener("click", (e) => {
  e.preventDefault();
  // Add your logic here to:
  // 1. Identify the element containing the username
  // 2. Alert if the input is empty.
  // 3. If the the input is not empty, console.log the name of the user.
  // Bonus Task: Print an error message in the div with the id "error".
});
