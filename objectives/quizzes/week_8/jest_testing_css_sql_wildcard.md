# Jest Testing, CSS Selectors, & SQL Wildcard

_Share a link to your completed version of this file with staff._

What are essential components of writing a jest test? There is more than one response.
- [ ] Creating a test file.
- [ ] Use the `describe` block to group related tests.
- [ ] Use `test` of `it` functions to define individual test cases
- [ ] Use the `expect` function inside of each test combined with matchers to assert the expected outcome of your code
- [ ] I am not really sure.

What is true about SQL Wildcard(s)? There is more than one response.
- [ ] They are used to substitute one or more characters in a string
- [ ] They are used with the LIKE operator
- [ ] They can never be used in a WHERE clause
- [ ] They can be combined with more than one wildcard in a row
- [ ] They are the regex characters for SQL

How can you run Jest? There is more than one response.
- [ ] npm test -- <script>
- [ ] jest <script>
- [ ] deploy your code so the deployment system will test
- [ ] Who knows!

How do you run a single jest file? There is more than one response.
- [ ] npm test -- <file_name_with_extension>
- [ ] jest  <file_name_with_extension>
- [ ] npm run start <file_name_with_extension>
- [ ] jest test <file_name_with_extension>
- [ ] Image title

Which is true about the coverage flag `npm test -- --coverage`? There is more than one response.

- [ ] This command is written incorrectly, there is no need for the double dash before the word coverage
- [ ] It is a generalized tool that checks which global functions work properly
- [ ] It isn't an option for npm itself, but rather is a parameter for the script that npm executes
- [ ] It executes the coverage report script after npm test is run

What is true about CSS attribute selectors? There is more than one response.
- [ ] They are selectors with specific matching condition; the css regex
- [ ] They are used to select javascript elements with an attribute value containing a specified word
- [ ] They are used to select all the elements that have the specified attribute and applies the CSS property to that attribute

What is being tested in this Jest report? There is more than one response.

<img width="773" height="356" alt="jest test" src="https://github.com/user-attachments/assets/475e97a8-b222-4d7f-bf4e-1bc4f0858163" />
- [ ] All the tests for the <SubHeader component />
- [ ] The component renders with a back button
- [ ] The component renders
- [ ] All the tests for the <Header component />

Is there anything wrong with this coverage testing. There is more than one response.
- [ ] No all tests in the test suite are passing; therefore all lines of code are covered.
- [ ] The user did not use the separating double dash after the npm command to invoke the script coverage report
- [ ] The coverage report shows that the project has zero lines; therefore no lines were covered.
- [ ] It checks for component render twice; this is redundant

What can you expect to see in the above example of updating multiple rows?

<img width="427" height="101" alt="jesttestingquizq11" src="https://github.com/user-attachments/assets/e0e2866f-86c4-4522-bea9-241be9fd810e" />
- [ ] changes the value of the country column to NP if age is 22; if there are more than one row where age is 22, all the matching rows will be edited
- [ ] change the value where the row contains age to 22 if there is a column that has a value of NP
- [ ] changes the value of the country column to NP if age is 22; if there are more than one row where age is 22, the first matching row will be edited
