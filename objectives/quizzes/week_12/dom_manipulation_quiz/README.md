Complete as many DOM Manipulation actions possible for each square in the sandbox to make the generated text work when there is user interaction.

DOM Event Resource: https://www.w3schools.com/jsref/dom_obj_event.asp

Box 1: Generate a random background color for the square when clicked.

Box 2: Change the square's background color to red when moused-over and change it back to the original grey color when moused-out. This will require two events.

Box 3: Append a CSS class called "image1" to show the image in your CSS when the square is double-clicked.

Box 4: Click & hold on the square to let the user view an inverted background-color. This will require two events.

Box 5: Animate the car icon to move right when clicked.

Box 6: Show hidden text when button is clicked.

Bonus: Use DOM Manipulation to showcase three new user interactions on boxes 7, 8, & 9. Change the text in each box to reflect the user interaction desired.
