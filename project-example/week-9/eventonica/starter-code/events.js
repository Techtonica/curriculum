const allEvents = [];

const addEvent = (/* arguments you decide go here */) => {
  // Adds a new Event
};

const updateEvent = () => {
  // Update existing Event
};

const deleteEvent = () => {
  // Deletes Event
};

const findEventsByDate = () => {
  // Return Event object instances in this.allEvents with a specified date
};

const findEventsbyCategory = () => {
  // Return Event object instances in this.allEvents with a specified category
};

export {
  allEvents,
  addEvent,
  updateEvent,
  deleteEvent,
  findEventsByDate,
  findEventsbyCategory
};
